#include <iostream>
#include <string>
using namespace std;

/**
   Return true if all elements in col are the same.
   @param board a 3 x 3 array of strings
   @param col the column to check
   @return true if all elements are the same
*/

bool check_col(string board[3][3], int col)
{
   return !col ;
}
